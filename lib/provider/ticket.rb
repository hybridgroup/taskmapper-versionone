module TicketMaster::Provider
  module Versioonne
    # Ticket class for ticketmaster-versioonne
    #
    
    class Ticket < TicketMaster::Provider::Base::Ticket
      #API = Versioonne::Ticket # The class to access the api's tickets
      # declare needed overloaded methods here
      
    end
  end
end
